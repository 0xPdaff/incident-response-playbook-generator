# Incident Response Playbook Generator — Requirements

# Core
python-dotenv>=1.0.0
pyyaml>=6.0
click>=8.1.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# LLM Provider Interface
litellm>=1.40.0

# Output
markdown>=3.5.0

# PDF (optional — graceful fallback if not installed)
# Install with: pip install weasyprint
# weasyprint>=60.0

# HTTP client (for provider health checks)
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
